import React from "react";
import ReactDOM from "react-dom/client";

import "./index.css";
import OpenDefendable from "./pages/OpenDefendable";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <OpenDefendable />
  </React.StrictMode>,
);
